import { pretendard } from "../lib/localfont"

export default function Footer() {
  return (
    <footer className={`${pretendard.className} text-text-700 text-sm mb-8`}>
      © 2024-2025 정솔미
    </footer>
  )
}